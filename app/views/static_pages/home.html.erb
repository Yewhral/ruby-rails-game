<% flash.each do |message_type, message| %>
    <%= content_tag(:div, message, class: "alert alert-#{message_type}") %>
<% end %>

<% if logged_in? %>
  <h1>Roadmap</h1>
  <nav>
    <div class="singular">
      <p>Links</p>
      <ul>
        <li><%= link_to "Message board", notices_path %></li>
      </ul>
    </div>
  </nav>
<% else %>
  <div class="singular">
    <h1>Sanctum of Snow</h1>
    Post some basic info here to tell what's this
  </div>
<% end %>
